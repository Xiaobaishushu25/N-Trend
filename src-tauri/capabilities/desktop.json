{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions for the main and standalone windows",
  "windows": ["main", "settings", "review", "notifications"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["autostart:default"]
}
